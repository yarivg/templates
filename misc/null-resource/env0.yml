version: 2
deploy:
  steps:
    setupVariables:
      after:
        - name: Install Bazel
          run: |
            echo "@testing https://dl-cdn.alpinelinux.org/alpine/edge/testing" | sudo tee -a /etc/apk/repositories
            apk update
            apk add --no-cache bazel6@testing
