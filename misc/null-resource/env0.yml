version: 2
deploy:
  steps:
    setupVariables:
      after:
        - name: Install Bazel
          run: |
            curl -O https://dl-cdn.alpinelinux.org/alpine/edge/testing/x86_64/bazel6-6.5.0-r0.apk
            sudo apk add --allow-untrusted bazel6-6.5.0-r0.apk